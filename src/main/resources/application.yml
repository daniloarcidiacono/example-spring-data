spring:
  main:
    banner-mode: 'off'

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/example_springdata?useSSL=false

  jpa:
    properties:
      hibernate.hbm2ddl.auto: validate
      hibernate.show_sql: true



